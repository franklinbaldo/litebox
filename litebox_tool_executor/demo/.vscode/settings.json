{
    "terminal.integrated.profiles.windows": {
        "LiteBox Sandbox": {
            "path": "powershell.exe",
            "args": ["-NoProfile", "-ExecutionPolicy", "Bypass", "-File", "C:\\src\\litebox\\litebox_tool_executor\\scripts\\litebox-shell.ps1"],
            "env": {
                "LITEBOX_ROOTFS": "C:\\src\\litebox\\target\\busybox-minimal.tar"
            },
            "icon": "shield"
        }
    },
    // Make the sandbox the default terminal so Copilot agent mode uses it
    // when running commands.
    "terminal.integrated.defaultProfile.windows": "LiteBox Sandbox"
}
